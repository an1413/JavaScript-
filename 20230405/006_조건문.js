let item = true ? console.log('true') : console.log('false');
console.log(item)

let score = 20;
const result = score >= 90 ? 'A' : score >=80 ? 'B' : score >= 70 ? 'C' : score >= 60 ? 'D' : '강해져서 돌아와라' 
console.log(result);